"""claude-code-langfuse-hook — Claude Code Stop hook → Langfuse tracer.

Public surface:
  - `claude-langfuse` CLI (entry point in pyproject.toml)
  - `claude_code_langfuse_hook.hook.run` — programmatic hook entry point
"""

__version__ = "0.5.0"
